[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v
asyncio_mode = strict

markers =
    integration: marks tests as integration tests requiring test database
    slow: marks tests as slow running tests

