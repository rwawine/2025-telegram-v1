# Диагностика проблем после внесения изменений

## Быстрая проверка

### 1. Перезапуск приложения
**ВАЖНО:** После любых изменений в коде необходимо перезапустить:
- Flask веб-сервер (если используете `run_flask.py` или `main.py`)
- Telegram бота

### 2. Проверка импортов
Убедитесь, что все модули доступны. Запустите:
```python
python -c "from bot.handlers.registration import RegistrationHandler; print('OK')"
python -c "from web.routes.admin import admin_bp; print('OK')"
python -c "from services.advanced_analytics_service import AdvancedAnalyticsService; print('OK')"
```

### 3. Проверка зависимостей
Убедитесь, что установлен `openpyxl`:
```bash
pip install openpyxl
```

## Конкретные проблемы

### Проблема: "Не работает обработка повторной заявки"

**Симптомы:** При повторном нажатии "подать заявку" все еще показывается "не совсем понял ваш запрос"

**Решение:**
1. Убедитесь, что бот перезапущен
2. Проверьте логи бота - должны быть сообщения о регистрации обработчиков
3. Проверьте, что файл `bot/states.py` содержит `repeat_submission_guard = State()`
4. Проверьте, что в `bot/handlers/registration.py` есть регистрация:
   ```python
   self.router.message.register(self.handle_repeat_submission, RegistrationStates.repeat_submission_guard)
   ```

### Проблема: "Не работает создание участника в админ-панели"

**Симптомы:** Кнопка не появляется, модальное окно не открывается, или форма не отправляется

**Решение:**
1. Проверьте, что вы залогинены как администратор
2. Откройте консоль браузера (F12) и проверьте на ошибки JavaScript
3. Проверьте, что форма отправляется на правильный URL
4. Проверьте логи Flask - должны быть сообщения о запросе к `/admin/participants/create`
5. Убедитесь, что в `database/admin_queries.py` есть метод `create_participant`

### Проблема: "Не работает экспорт в Excel"

**Симптомы:** Кнопка не работает, файл не скачивается, или ошибка при генерации

**Решение:**
1. Проверьте, что установлен `openpyxl`: `pip list | grep openpyxl`
2. Проверьте права на запись в директорию для временных файлов
3. Проверьте логи Flask на наличие ошибок
4. Убедитесь, что в базе данных есть данные для экспорта

### Проблема: "Не принимаются карты с 13 цифрами"

**Симптомы:** Система все еще требует ровно 16 цифр

**Решение:**
1. Убедитесь, что бот перезапущен после изменений
2. Проверьте, что в `utils/validators.py` регулярное выражение: `r"^[0-9]{13}$|^[0-9]{16}$"`
3. Проверьте, что функция `validate_loyalty_card` очищает пробелы и дефисы

## Проверка через логи

### Логи бота
Смотрите `logs/app.log` на наличие ошибок при обработке сообщений:
```bash
tail -f logs/app.log | grep -i error
```

### Логи Flask
Проверьте вывод Flask сервера на наличие ошибок при запросах к админ-панели.

## Тестирование вручную

### Тест 1: Повторная заявка
1. Зарегистрируйте тестового пользователя
2. Попробуйте снова нажать "подать заявку"
3. **Ожидаемый результат:** Сообщение о текущем статусе, а не fallback

### Тест 2: Создание участника
1. Войдите в админ-панель
2. Перейдите в "Участники"
3. Нажмите "➕ Добавить участника"
4. Заполните форму (можно без Telegram ID)
5. Отправьте форму
6. **Ожидаемый результат:** Участник появляется в списке

### Тест 3: Экспорт базового отчета
1. Перейдите в "Аналитика"
2. Нажмите "📊 Экспорт в Excel"
3. **Ожидаемый результат:** Скачивается файл `.xlsx` с несколькими листами

### Тест 4: Экспорт расширенного отчета
1. Перейдите в "Расширенная аналитика"
2. Нажмите "Экспорт в Excel"
3. **Ожидаемый результат:** Скачивается файл `.xlsx` с листами: Сводка, Воронка, Временной ряд, Тепловая карта, Когорты

### Тест 5: Валидация карт
1. Начните регистрацию
2. На шаге "Карта лояльности" введите 13 цифр
3. **Ожидаемый результат:** Принимается
4. Попробуйте 16 цифр
5. **Ожидаемый результат:** Тоже принимается

## Если ничего не помогает

1. Проверьте, что все файлы сохранены
2. Убедитесь, что нет синтаксических ошибок: `python -m py_compile <файл>`
3. Проверьте версии зависимостей в `requirements.txt`
4. Попробуйте очистить кэш Python: `find . -type d -name __pycache__ -exec rm -r {} +`
5. Переустановите зависимости: `pip install -r requirements.txt --force-reinstall`

